<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= event.title || 'Event' %></title>
  <link rel="stylesheet" href="/main.css">
</head>
<body>
  <h1><%= event.title || '(no title)' %></h1>
  <p><em><%= event.event_date %></em></p>
  <p><%= event.description %></p>
  <h2>Tickets</h2>
  <ul>
    <% tickets.forEach(t => { %>
      <li><strong><%= t.type %>:</strong> £<%= t.price.toFixed(2) %> (qty. <%= t.quantity %>)</li>
    <% }) %>
  </ul>
  <p><a href="/">← Home</a></p>
</body>
</html>
